APP_NAME=sikessem
APP_TITLE=Sikessem
APP_DESCRIPTION="Sikessem's Web Application"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_LOCALE=fr
APP_FALLBACK_LOCALE=fr
APP_FAKER_LOCALE=fr_CI
APP_TIMEZONE=UTC
APP_SCHEME=https
APP_HOST=127.0.0.1
APP_PORT=8000
APP_URL=${APP_SCHEME}://${APP_HOST}:${APP_PORT}
APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

ADMIN_NAME="Sigui Kessé Emmanuel"
ADMIN_EMAIL=dev@sikessem.com
ADMIN_USERNAME=siguici
ADMIN_PASSWORD=
ADMIN_ROLE=developer
ADMIN_GUARD=admin

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=${APP_HOST}
DB_PORT=3306
DB_DATABASE=${APP_NAME}
DB_USERNAME=${ADMIN_USERNAME}
DB_PASSWORD=${ADMIN_PASSWORD}

PG_CONNECTION=pgsql
PG_HOST=${DB_HOST}
PG_PORT=5432
PG_DATABASE=${DB_DATABASE}
PG_USERNAME=${DB_USERNAME}
PG_PASSWORD=${DB_PASSWORD}
PG_SCHEMA=public

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=${APP_HOST}

REDIS_HOST=${APP_HOST}
REDIS_PASSWORD=${ADMIN_PASSWORD}
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=${APP_HOST}
MAIL_PORT=2525
MAIL_USERNAME=${ADMIN_USERNAME}
MAIL_PASSWORD=${ADMIN_PASSWORD}
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=${ADMIN_EMAIL}
MAIL_FROM_NAME="${APP_TITLE}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

REVERB_APP_ID=
REVERB_APP_KEY=
REVERB_APP_SECRET=
REVERB_HOST=${APP_HOST}
REVERB_PORT=${APP_PORT}
REVERB_SCHEME=${APP_SCHEME}

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
VITE_APP_NAME="${APP_TITLE}"
